const getNotes = function () {
  return "Your notes...";
};

const addNote = function (title, body) {};

const loadNotes = function () {};

module.exports = {
  getNotes: getNotes,
  addNote: addNote,
};
